interface:
  display_name: "Workflow"
  short_description: "Choose and compose multi-agent workflows"
  default_prompt: "Use $workflow to decide whether delegation pays for this task; stay single-agent when sufficient, otherwise choose the smallest useful workflow."
policy:
  allow_implicit_invocation: true
